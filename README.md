# Just a quick little generator for Emily and Kevin

Enter actual questions into `questions.txt`, newline delimited.

Make sure you have `node` installed and run:

```
./generate
```

Like magic! A nice little HTML document in `build/`.